# Paste into the Advanced tab of the Meta Education Proxy Host in NPM.
# Domain: meta-edu.ru; forward: http://meta-education:3000.
client_max_body_size 22m;
proxy_buffering off;
proxy_read_timeout 60s;
proxy_send_timeout 60s;
